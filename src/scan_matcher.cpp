#include "simple_scan_matcher/scan_matcher.h"

namespace simple_scan_matcher
{

  void ScanMatcher::computeTransform(const Scan& source, const Scan& taget)
  {
    if (source.empty() || taget.empty()) return;
  }

}
